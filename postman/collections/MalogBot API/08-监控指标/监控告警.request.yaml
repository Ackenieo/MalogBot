$kind: http-request
name: 监控告警
method: GET
url: '{{base_url}}/monitoring/alerts'
description: 获取监控告警列表，返回 status, alerts, count
order: 4000
queryParams:
  - key: limit
    value: '{{limit}}'
    description: 返回条数限制，默认 20
