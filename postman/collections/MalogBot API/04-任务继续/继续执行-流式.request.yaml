$kind: http-request
name: 继续执行-流式
method: POST
url: '{{base_url}}/continue/stream'
description: |-
  继续执行上一个任务（流式 SSE），返回 SSE 流式数据。
  每个 chunk 格式为 data: {...}\n\n
order: 2000
headers:
  - key: Content-Type
    value: application/json
