$kind: http-request
name: 首次引导回复
method: POST
url: '{{base_url}}/onboarding/reply'
description: 首次使用时的引导回复接口，返回 type, output, onboarding_completed
order: 6000
headers:
  - key: Content-Type
    value: application/json
body:
  type: json
  content: |-
    {
      "message": "我是一名后端开发工程师，主要使用 Python"
    }
